import React, { useRef } from 'react';
import './style.scss';

const Header = () => {
  return (
    <div className='header'>
      <p>Yoga sequence builder</p>
    </div>
  );
}

export default Header;